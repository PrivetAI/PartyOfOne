workflows:
  ios-native-quick-start:
    name: iOS Native
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: 'Party of One'
    environment:
      ios_signing:
        # Схема Codemagic требует либо пару distribution_type + bundle_identifier,
        # либо список provisioning_profiles. Загрузить профиль в хранилище
        # нельзя, поэтому берём автоматическую пару, а сертификат называем явно,
        # чтобы Codemagic взял тот, чей приватный ключ у него есть.
        distribution_type: app_store
        bundle_identifier: com.karlokovacic.partyofone
        certificates:
          - 'Party of One'
      vars:
        BUNDLE_ID: "com.karlokovacic.partyofone"
        XCODE_WORKSPACE: "Party of One.xcodeproj"
        XCODE_SCHEME: "Party of One"
      xcode: latest
    scripts:
      # Профили забираем прямо из Apple по ключу ASC. list-profiles --save
      # кладёт их в ~/Library/MobileDevice/Provisioning Profiles и, в отличие
      # от fetch-signing-files, не требует приватного ключа сертификата.
      - name: Fetch provisioning profiles
        script: |
          app-store-connect list-profiles \
            --bundle-id-identifier "$BUNDLE_ID" \
            --profile-type IOS_APP_STORE \
            --save
      - name: Set up provisioning profiles settings on Xcode project
        script: xcode-project use-profiles
      - name: Build ipa for distribution
        script: |
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: false
